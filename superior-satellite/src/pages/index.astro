---
import { getCollection } from "astro:content";
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getCollection("post");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);
---

<PageLayout meta={{ title: "Home" }}>
	<section class="mb-8">
		<h1 class="title mb-6">Welcome, traveler.</h1>
		<p class="mb-6">
			I'm a senior at at Brown University studying Math and Computer Science. 
		</p>
		<p class="mb-6">
			My academic interests lie in cryptography, formal methods, and computer systems.
		</p>
		<p class="mb-6">
			Currently, you can reach me through:
			<ul id="socials">
				<li>
					email: <a href = "mailto: jackcheng2002@gmail.com">jackcheng2002@gmail.com</a>
				</li>
			</ul>
		</p>
	</section>
	<!-- <section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">Writing</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section> -->
</PageLayout>
